<div id="<%= dom_id video %>">
  <p>
    <strong>Title:</strong>
    <%= video.title %>
  </p>

  <% if video.video.attached? %>
    <%= video_tag url_for(video.video), controls: true %>
  <% end %>

</div>
